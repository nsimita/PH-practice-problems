#include <stdio.h>
#include <string.h>

int main()

{
    char a [100]= "air";
    char b[100]= "air";
   
    int comp = strcmp(b,a);
    printf("%d",comp);


    // if prints negative number then a<b;
    //if prints positive number then a>b;
    // if prints 0 then a=b;
    return 0;
}
